namespace Matchmancer.Lore
{
    /// <summary>
    /// Grouping tabs in the Lore Gallery. Entries are filtered by category
    /// when the player selects a tab.
    /// </summary>
    public enum LoreCategory
    {
        Character   = 0,
        Enemy       = 1,
        World       = 2,
        School      = 3,  // Schools of Magick
        Item        = 4,
        History     = 5,
    }
}
